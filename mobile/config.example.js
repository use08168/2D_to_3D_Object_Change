// config.js 템플릿 — 이 파일을 config.js로 복사한 뒤 PC의 Tailscale IP를 넣으세요.
//   cp config.example.js config.js
// (config.js는 .gitignore에 포함 — 실제 IP는 저장소에 올라가지 않음)
export const SERVER = 'ws://<PC_TAILSCALE_IP>:8765';
